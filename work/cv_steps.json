[
  { "click": ".map[data-m=city]" },
  { "wait": 3000 },
  { "click": "#startb" },
  { "wait": 800 },
  { "js": "(() => { const D = window.__DEBUG__, w = D.world, G = D.G, t = D.track; const orig = w.City.cityChunk; const S = window.__STEPS__ = []; const City = Object.assign({}, w.City); City.cityChunk = function* (w2, ch) { const it = orig(w2, ch); let k = 0; for (;;) { const t0 = performance.now(); const r = it.next(); S.push([Math.round((performance.now() - t0) * 100) / 100, k++, ch.c]); if (r.done) break; yield; } }; w.City = City; window.__FT__ = []; window.__AUTOPILOT__ = (dt) => { const c = D.car, n = t.nearest(c.x, c.z, G.idx), ahead = t.sample(G.s + 10 + c.speed * 0.6); let e = ahead.h - c.yaw; e = Math.atan2(Math.sin(e), Math.cos(e)); const k = Math.abs(t.sample(G.s + 25).k); const target = k > 0.03 ? 13 : k > 0.012 ? 19 : 30; const steer = Math.max(-1, Math.min(1, e * 2.4 - n.u * 0.12)); return { throttle: c.speed < target ? 1 : 0, brake: c.speed > target + 4 ? 1 : 0, steer, hand: k > 0.035 && c.speed > 14 ? 1 : 0, reverse: false, touch: false }; }; window.__ONFRAME__ = (dt, real) => { window.__FT__.push([Math.round(performance.now()), Math.round(real * 1000), Math.round(G.s)]); }; window.__T0__ = performance.now(); })()" },
  { "wait": 30000 },
  { "js": "(() => { const S = window.__STEPS__.slice().sort((a, b) => b[0] - a[0]); const byK = {}; for (const [ms, k] of window.__STEPS__) { const b = byK[k] || (byK[k] = { n: 0, max: 0, sum: 0 }); b.n++; b.sum += ms; b.max = Math.max(b.max, ms); } for (const k in byK) byK[k].avg = Math.round(byK[k].sum / byK[k].n * 100) / 100; const f = window.__FT__.filter((x) => x[0] > window.__T0__ + 1000); const ms = f.map((x) => x[1]).sort((a, b) => a - b); return { top: S.slice(0, 12), byStep: byK, frames: f.length, p50: ms[Math.floor(ms.length / 2)], p99: ms[Math.floor(ms.length * 0.99)], max: ms[ms.length - 1], long: f.filter((x) => x[1] > 25).slice(0, 12) }; })()" }
]
