[
  { "click": ".diff[data-d=hard]" },
  { "wait": 3000 },
  { "click": "#startb" },
  { "wait": 1200 },
  { "js": "(() => { const D = window.__DEBUG__, G = D.G, t = D.track, car = D.car; D.rainNow(0); const f = t.features.filter((q) => q.type === 'hairpin' && t.pts[q.i0].s > 250)[0]; let kmax = 0; for (let i = f.i0; i <= f.i1; i++) kmax = Math.max(kmax, Math.abs(t.pts[i].k)); let ie = f.i0; while (ie < f.i1 && Math.abs(t.pts[ie].k) < 0.6 * kmax) ie++; const sE = t.pts[ie].s; D.teleport(sE - 45, 60); let T = 0, flick = -1; window.__AUTOPILOT__ = (dt) => { T += dt; const c = car, n = t.nearest(c.x, c.z, G.idx); const ahead = t.sample(G.s + 7 + c.speed * 0.45); let e = ahead.h - c.yaw; e = Math.atan2(Math.sin(e), Math.cos(e)); const kd = Math.sign(t.sample(sE + 5).k); if (flick < 0 && G.s > sE - 7) flick = T; const inFlick = flick >= 0 && T - flick < 0.35; const steer = inFlick ? kd : Math.max(-1, Math.min(1, e * 2.2 - n.u * 0.08)); return { throttle: inFlick ? 0.3 : (G.s < sE - 7 ? (c.speed < 16.7 ? 1 : 0) : 0.75), brake: 0, steer, hand: inFlick ? 1 : 0, reverse: false, touch: false }; }; window.__TR__ = () => ({ k: +(G.trailK || 0).toFixed(2), beta: +car.beta.toFixed(2), pts: D.trails ? D.trails.trails.map((x) => x.pts.length) : null }); return Math.round(sE); })()" },
  { "wait": 2350 }, { "shot": "L1" }, { "js": "window.__TR__()" },
  { "wait": 250 }, { "shot": "L2" }, { "js": "window.__TR__()" },
  { "wait": 250 }, { "shot": "L3" }, { "js": "window.__TR__()" },
  { "wait": 300 }, { "shot": "L4" }, { "js": "window.__TR__()" }
]
