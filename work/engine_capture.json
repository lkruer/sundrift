[
  { "click": ".diff[data-d=hard]" },
  { "wait": 3000 },
  { "click": "#startb" },
  { "wait": 900 },
  { "js": "(() => { const D = window.__DEBUG__, A = D.audio, c = A.ctx; D.rainNow(0); A.music.on = false; if (A.musicGain) A.musicGain.gain.value = 0; const sp = c.createScriptProcessor(4096, 1, 1); const chunks = []; sp.onaudioprocess = (e) => { const d = e.inputBuffer.getChannelData(0); const m = new Float32Array(d.length / 2); for (let i = 0; i < m.length; i++) m[i] = d[2 * i]; chunks.push(m); }; A.master.connect(sp); const z = c.createGain(); z.gain.value = 0; sp.connect(z); z.connect(c.destination); window.__CAP__ = chunks; D.teleport(420, 0); const t = D.track, G = D.G, car = D.car; let T = 0; const log = []; window.__AUTOPILOT__ = (dt) => { T += dt; const n = t.nearest(car.x, car.z, G.idx), ahead = t.sample(G.s + 10 + car.speed * 0.6); let e = ahead.h - car.yaw; e = Math.atan2(Math.sin(e), Math.cos(e)); const steer = Math.max(-1, Math.min(1, e * 2.4 - n.u * 0.12)); if (Math.floor(T * 10) !== Math.floor((T - dt) * 10)) log.push([+T.toFixed(1), G.gear ? G.gear.gear : -1, Math.round(G.gear ? G.gear.rpm : 0), Math.round(car.kmh)]); if (T < 6) return { throttle: 1, brake: 0, steer, hand: 0, reverse: false, touch: false }; if (T < 7.6) return { throttle: 0, brake: 0, steer, hand: 0, reverse: false, touch: false }; if (T < 8.0) return { throttle: 0.4, brake: 0, steer: 1, hand: 1, reverse: false, touch: false }; return { throttle: 1, brake: 0, steer: -0.6, hand: 0, reverse: false, touch: false }; }; window.__LOG__ = log; return { worklet: !!A.engNode, sr: c.sampleRate }; })()" },
  { "wait": 10500 },
  { "js": "(() => { window.__AUTOPILOT__ = null; const ch = window.__CAP__; const n = ch.reduce((a, b) => a + b.length, 0); const out = new Int16Array(n); let o = 0; for (const b of ch) for (const v of b) out[o++] = Math.max(-32767, Math.min(32767, Math.round(v * 32767))); let s = ''; const u8 = new Uint8Array(out.buffer); for (let i = 0; i < u8.length; i += 32768) s += String.fromCharCode.apply(null, u8.subarray(i, i + 32768)); window.__B64__ = btoa(s); return { n, worklet: !!window.__DEBUG__.audio.engNode, log: window.__LOG__.filter((x, i) => i % 5 === 0) }; })()" },
  { "js": "window.__B64__" }
]
