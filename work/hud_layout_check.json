[
  { "wait": 2500 },
  { "js": "document.getElementById('startb').click()" },
  { "wait": 800 },
  { "js": "(() => { const h = window.__DEBUG__.hud, sizes = [[360,640],[375,667],[390,844],[430,932],[320,568],[640,360],[667,375],[844,390],[932,430],[568,320],[768,1024],[1024,768],[820,1180],[1180,820],[1280,720],[1366,768],[1440,900],[1920,1080],[2560,1440],[800,600],[1024,1024],[600,900],[500,800]]; const out = []; const keys = ['score','combo','time','btn0','btn1','dash','angle','pad','co']; const ov = (a, b) => a.x < b.x + b.w - 1 && b.x < a.x + a.w - 1 && a.y < b.y + b.h - 1 && b.y < a.y + a.h - 1; const was = document.body.classList.contains('touch'); for (const touch of [false, true]) { document.body.classList.toggle('touch', touch); for (const [w, hh] of sizes) { h.touch = touch; h.resize(w, hh, 1); const b = h.rects(); const bad = []; for (let i = 0; i < keys.length; i++) for (let j = i + 1; j < keys.length; j++) { const A = b[keys[i]], B = b[keys[j]]; if (!A || !B) continue; if (!touch && (keys[i] === 'pad' || keys[j] === 'pad')) continue; if (keys[i] === 'angle' && keys[j] === 'co') continue; if (ov(A, B)) bad.push(keys[i] + '/' + keys[j]); } for (const k of keys) { const A = b[k]; if (!A || (!touch && k === 'pad')) continue; if (A.x < 0 || A.y < 0 || A.x + A.w > b.W || A.y + A.h > b.H) bad.push(k + ' off-screen'); } if (bad.length) out.push((touch ? 'T ' : 'D ') + w + 'x' + hh + ': ' + bad.join(', ')); } } document.body.classList.toggle('touch', was); h.touch = was; h.resize(innerWidth, innerHeight, window.__DEBUG__.renderer.getPixelRatio()); return out.length ? out : 'no overlaps'; })()" }
]
