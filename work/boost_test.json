[
  { "click": ".diff[data-d=hard]" },
  { "wait": 3000 },
  { "click": "#startb" },
  { "wait": 1200 },
  { "js": "(async () => { const D = window.__DEBUG__, G = D.G, t = D.track, car = D.car, S = D.scoring; const sleep = (ms) => new Promise((r) => setTimeout(r, ms)); const f = t.features.filter((q) => q.type === 'hairpin' && t.pts[q.i0].s > 250)[0]; let kmax = 0; for (let i = f.i0; i <= f.i1; i++) kmax = Math.max(kmax, Math.abs(t.pts[i].k)); let ie = f.i0; while (ie < f.i1 && Math.abs(t.pts[ie].k) < 0.6 * kmax) ie++; const sE = t.pts[ie].s; D.teleport(sE - 45, 60); let T = 0, flick = -1, pull = -1; const log = []; let slideEnd = -1, boostAt = -1, cutAt = -1; window.__AUTOPILOT__ = (dt) => { T += dt; const c = car, n = t.nearest(c.x, c.z, G.idx); const ahead = t.sample(G.s + 7 + c.speed * 0.45); let e = ahead.h - c.yaw; e = Math.atan2(Math.sin(e), Math.cos(e)); const kd = Math.sign(t.sample(sE + 5).k); if (flick < 0 && G.s > sE - 7) flick = T; const inFlick = flick >= 0 && T - flick < 0.35; if (slideEnd < 0 && flick >= 0 && T - flick > 0.6 && Math.abs(c.beta) < 0.11) slideEnd = T; if (boostAt < 0 && c.boost > 0.05) { boostAt = T; pull = T + 0.5; } if (cutAt < 0 && pull > 0 && T > pull + 0.05 && c.boost < 0.01) cutAt = T; const hand = inFlick || (pull > 0 && T > pull && T < pull + 0.25) ? 1 : 0; const steer = inFlick ? kd : Math.max(-1, Math.min(1, e * 2.2 - n.u * 0.08)); if (Math.floor(T * 10) !== Math.floor((T - dt) * 10)) log.push([+T.toFixed(1), +c.beta.toFixed(2), +c.boost.toFixed(2), S.active ? 1 : 0, hand]); return { throttle: inFlick ? 0.3 : (G.s < sE - 7 ? (c.speed < 16.7 ? 1 : 0) : 0.75), brake: 0, steer, hand, reverse: false, touch: false }; }; await sleep(6500); window.__AUTOPILOT__ = null; return { flick: +flick.toFixed(2), slideEnd: +slideEnd.toFixed(2), boostAt: +boostAt.toFixed(2), boostDelay: +(boostAt - slideEnd).toFixed(2), pulledAt: +pull.toFixed(2), cutAt: +cutAt.toFixed(2), log: log.filter((x, i) => i % 3 === 0) }; })()" }
]
