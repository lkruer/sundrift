[
  { "click": ".map[data-m=city]" },
  { "wait": 4000 },
  { "click": "#startb" },
  { "wait": 1500 },
  { "js": "(() => { const D = window.__DEBUG__; D.G.hour = 22; D.G.hourShown = 22; D.rainNow(0); D.teleport(420, 0); window.__EMIT__ = (kind, k) => { const out = []; D.world.root.traverse((o) => { if (o.name === 'steam' && o.geometry) { const P = o.geometry.attributes.position, A = o.geometry.attributes.aP, Dd = o.geometry.attributes.aDir; for (let i = 0; i < P.count; i += 4) if (Math.round(A.getZ(i)) === kind) out.push([P.getX(i), P.getY(i), P.getZ(i), Dd.getX(i), Dd.getY(i)]); } }); const c = D.car; const uniq = []; for (const e of out) if (!uniq.some((u) => Math.hypot(u[0] - e[0], u[2] - e[2]) < 0.5)) uniq.push(e); uniq.sort((a, b) => Math.hypot(a[0] - c.x, a[2] - c.z) - Math.hypot(b[0] - c.x, b[2] - c.z)); return uniq[k] || null; }; })()" },
  { "wait": 500 },
  { "js": "(() => { const e = window.__EMIT__(0, 0); if (!e) return 'none'; const D = window.__DEBUG__, p = D.track.sample(D.track.nearest(e[0], e[2], D.G.idx).s); const fx = Math.sin(p.h), fz = Math.cos(p.h); window.__CAM__ = { pos: [e[0] - fx * 9, e[1] + 1.8, e[2] - fz * 9], look: [e[0], e[1] + 1.4, e[2]], fov: 60 }; return e; })()" },
  { "wait": 1500 },
  { "shot": "H1_manhole" },
  { "js": "(() => { const e = window.__EMIT__(1, 1); if (!e) return 'none'; window.__CAM__ = { pos: [e[0] + e[3] * 7 + e[4] * 4, e[1] - 0.5, e[2] + e[4] * 7 - e[3] * 4], look: [e[0] + e[3], e[1] + 0.7, e[2] + e[4]], fov: 60 }; return e; })()" },
  { "wait": 1200 },
  { "shot": "H2_vent" },
  { "js": "(() => { const D = window.__DEBUG__; let best = null; D.world.root.traverse((o) => { if (o.name === 'signal lamps' && o.geometry) { const P = o.geometry.attributes.position; const x = P.getX(0), y = P.getY(0), z = P.getZ(0); const c = D.car; const d = Math.hypot(x - c.x, z - c.z); if (!best || d < best[3]) best = [x, y, z, d]; } }); if (!best) return 'no signals'; const [x, y, z] = best; window.__SIG__ = best; window.__CAM__ = { pos: [x + 5, y - 2.5, z + 5], look: [x, y, z], fov: 30 }; return best; })()" },
  { "wait": 800 },
  { "shot": "H3_signal_a" },
  { "wait": 6000 },
  { "shot": "H4_signal_b" },
  { "wait": 6000 },
  { "shot": "H5_signal_c" },
  { "js": "(() => { const D = window.__DEBUG__; window.__CAM__ = null; D.teleport(1500, 40); })()" },
  { "keys": ["KeyW"], "ms": 2500 },
  { "shot": "H6_drive" },
  { "js": "(() => { const D = window.__DEBUG__; const A = D.world.citySky.userData.airship; A.g.updateMatrixWorld(true); const e = A.g.matrixWorld.elements; const c = D.car; const y = D.world.ground.height(c.x, c.z); const dx = e[12] - c.x, dz = e[14] - c.z, L = Math.hypot(dx, dz); window.__CAM__ = { pos: [c.x - dx / L * 6, y + 2.5, c.z - dz / L * 6], look: [c.x + dx / L * 100, y + 2.5 + (e[13] - y) / L * 100 * 0.55, c.z + dz / L * 100], fov: 62 }; return [Math.round(L), Math.round(e[13] - y)]; })()" },
  { "wait": 900 },
  { "shot": "H7_airship_street" }
]
