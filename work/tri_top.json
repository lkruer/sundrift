[
  { "click": "#startb" },
  { "wait": 1200 },
  { "js": "window.__DEBUG__.teleport(2300, 30)" }, { "wait": 1500 },
  { "js": "(() => { const D = window.__DEBUG__; const list = []; D.scene.traverseVisible((o) => { if (!o.isMesh) return; const g = o.geometry; let t = (g.index ? g.index.count : g.attributes.position.count) / 3; const n = o.isInstancedMesh ? o.count : 1; list.push([Math.round(t * n), Math.round(t), n, o.name, o.material && o.material.name, o.parent && o.parent.name, o.material && o.material.type]); }); list.sort((a, b) => b[0] - a[0]); const unnamed = list.filter((r) => !r[3] && !r[4] && !r[5]); return { top: list.slice(0, 12), unnamed: unnamed.slice(0, 12) }; })()" }
]
