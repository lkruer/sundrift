[
  {"click": ".map[data-m=city]"},
  {"wait": 9000},
  {"click": "#startb"},
  {"wait": 1500},
  {"js": "(() => { const D = window.__DEBUG__, A = D.audio, c = A.ctx, C = A.cy; if (!C) return 'no city sound'; window.__AUTOPILOT__ = () => ({ throttle: 0, brake: 1, steer: 0, hand: 0, reverse: false, touch: false }); C.hornIn = C.passIn = C.sirenIn = 1e9; const kw = (src) => { const s = c.createBiquadFilter(); s.type = 'highshelf'; s.frequency.value = 1681; s.gain.value = 4; const h = c.createBiquadFilter(); h.type = 'highpass'; h.frequency.value = 38; h.Q.value = 0.5; src.connect(s); s.connect(h); return h; }; const m = c.createChannelMerger(12); let k = 0; for (const src of [A.musicGain, C.bus, A.master]) for (const node of [src, kw(src)]) { const sp = c.createChannelSplitter(2); node.connect(sp); sp.connect(m, 0, k++); sp.connect(m, 1, k++); } const sp = c.createScriptProcessor(1024, 12, 1); m.connect(sp); const z = c.createGain(); z.gain.value = 0; sp.connect(z); z.connect(c.destination); window.__CL__ = { phase: 'bed', rows: [] }; sp.onaudioprocess = (e) => { const ib = e.inputBuffer, n = ib.length, row = [window.__CL__.phase, +c.currentTime.toFixed(3)]; for (let p = 0; p < 6; p++) { const L = ib.getChannelData(2 * p), R = ib.getChannelData(2 * p + 1); let s = 0; for (let i = 0; i < n; i++) s += L[i] * L[i] + R[i] * R[i]; row.push(s / (2 * n)); } const Cy = A.cy; row.push(Cy && Cy.tOn ? +Cy.tD.toFixed(1) : -1, Cy && Cy.sOn ? +Cy.cD.toFixed(1) : -1); window.__CL__.rows.push(row); }; window.__CLSP__ = sp; return { sr: c.sampleRate, state: c.state }; })()"},
  {"wait": 20000},
  {"js": "(() => { const A = window.__DEBUG__.audio, C = A.cy; C.loG.disconnect(); C.midG.disconnect(); C.passIn = 0; window.__CL__.phase = 'pass'; return 'pass'; })()"},
  {"wait": 25000},
  {"js": "(() => { const A = window.__DEBUG__.audio; A.cy.passIn = 1e9; window.__CL__.phase = 'gap'; return 'gap'; })()"},
  {"wait": 7500},
  {"js": "(() => { const A = window.__DEBUG__.audio; window.__CL__.phase = 'horn'; let n = 0; A._horn(); window.__HI__ = setInterval(() => { if (++n < 8) A._horn(); else clearInterval(window.__HI__); }, 2500); return 'horn'; })()"},
  {"wait": 21000},
  {"js": "(() => { const A = window.__DEBUG__.audio; window.__CL__.phase = 'siren'; A._siren(); setTimeout(() => A._siren(), 10500); return 'siren'; })()"},
  {"wait": 21500},
  {"js": "(() => { const D = window.__DEBUG__, w = D.world, A = D.audio, tr = D.track; window.__CL__.phase = 'chirp'; let best = null; for (const [o, list] of w.colsBy) for (const r of list) if (r.name === 'signal' && r.r < 0.19) { let bs = 0, bd = 1e9; for (let s = 40; s < 900; s += 2) { const p = tr.sample(s); const d = Math.hypot(p.x - r.x, p.z - r.z); if (d < bd) { bd = d; bs = s; } } if (!best || bd < best.d) best = { s: bs, d: +bd.toFixed(1), x: Math.round(r.x), z: Math.round(r.z) }; } window.__SIG__ = best; window.__KIND__ = 0; D.teleport(best.s - 30, 0); window.__ONFRAME__ = () => { const C = A.cy; if (C && C.sOn) { C.sigPh = C.sigCycle - (A.ctx.currentTime % C.sigCycle); if (C.sigKind !== window.__KIND__) { C.sigKind = window.__KIND__; C.sigF = C.sigKind ? 1040 : 2700; C.sigStep = C.sigKind ? 1.1 : 0.42; } } }; return best; })()"},
  {"wait": 4500},
  {"js": "(() => { const D = window.__DEBUG__; D.teleport(window.__SIG__.s - 18, 0); return 1; })()"},
  {"wait": 4500},
  {"js": "(() => { const D = window.__DEBUG__; D.teleport(window.__SIG__.s - 7, 0); return 1; })()"},
  {"wait": 4500},
  {"js": "(() => { const D = window.__DEBUG__; window.__KIND__ = 1; window.__CL__.phase = 'chirpK'; D.teleport(window.__SIG__.s - 30, 0); return 1; })()"},
  {"wait": 4500},
  {"js": "(() => { const D = window.__DEBUG__; D.teleport(window.__SIG__.s - 18, 0); return 1; })()"},
  {"wait": 4500},
  {"js": "(() => { const D = window.__DEBUG__; D.teleport(window.__SIG__.s - 7, 0); return 1; })()"},
  {"wait": 4500},
  {"js": "(() => { const D = window.__DEBUG__, w = D.world; window.__CL__.phase = 'train'; window.__ONFRAME__ = null; const st = w._rail.list[0]; const sc = (st.s0 + st.s1) / 2; D.teleport(sc, 0); const inset = 21.5, L = st.s1 - st.s0 + 2 * inset, run = (L + 120) / 14; const u0 = (sc - st.s0 + inset + 59.75 - 14 * 14) / 14; const t0 = performance.now() / 1000, tt0 = u0 - st.i * 37; window.__ONFRAME__ = () => { w._trainT = tt0 + (performance.now() / 1000 - t0); }; return { sc: Math.round(sc), run: +run.toFixed(1), u0: +u0.toFixed(1) }; })()"},
  {"wait": 28000},
  {"js": "(() => { const R = window.__CL__.rows; window.__CLSP__.onaudioprocess = null; window.__ONFRAME__ = null; const db = (p) => 10 * Math.log10(p + 1e-20); const r1 = (x) => Math.round(x * 10) / 10; const mean = (a) => a.reduce((x, y) => x + y, 0) / Math.max(1, a.length); const mus = mean(R.map((r) => r[2])), musK = mean(R.map((r) => r[3])); const rel = (p, k) => r1(db(p) - db(k ? musK : mus)); const ph = (name, skip = 0) => { const rows = R.filter((r) => r[0] === name); const t0 = rows.length ? rows[0][1] : 0; return rows.filter((r) => r[1] - t0 >= skip); }; const movmax = (rows, col, n) => { let best = 0; for (let i = 0; i + n <= rows.length; i++) { let s = 0; for (let j = 0; j < n; j++) s += rows[i + j][col]; best = Math.max(best, s / n); } return best; }; const movmin = (rows, col, n) => { let best = Infinity; for (let i = 0; i + n <= rows.length; i++) { let s = 0; for (let j = 0; j < n; j++) s += rows[i + j][col]; best = Math.min(best, s / n); } return best; }; const out = { note: 'dB of stereo power relative to the music over the whole run; K = K-weighted', musicDbFS: r1(db(mus)), musicKDbFS: r1(db(musK)), masterDbFS: r1(db(mean(R.map((r) => r[6])))) }; const bed = ph('bed', 3); out.bed = { mean: rel(mean(bed.map((r) => r[4]))), meanK: rel(mean(bed.map((r) => r[5])), 1), loudest1s: rel(movmax(bed, 4, 47)), quietest1s: rel(movmin(bed, 4, 47)) }; const pass = ph('pass', 1); out.passBy = { mean: rel(mean(pass.map((r) => r[4]))), meanK: rel(mean(pass.map((r) => r[5])), 1), loudest300ms: rel(movmax(pass, 4, 14)), loudest300msK: rel(movmax(pass, 5, 14), 1) }; const horn = ph('horn'); const ht0 = horn.length ? horn[0][1] : 0; out.hornsLoudest100ms = []; out.hornsLoudest100msK = []; for (let k = 0; k < 8; k++) { const w = horn.filter((r) => r[1] - ht0 >= k * 2.5 && r[1] - ht0 < (k + 1) * 2.5); out.hornsLoudest100ms.push(rel(movmax(w, 4, 5))); out.hornsLoudest100msK.push(rel(movmax(w, 5, 5), 1)); } const sir = ph('siren'); const st0 = sir.length ? sir[0][1] : 0; out.sirens = [0, 1].map((k) => { const w = sir.filter((r) => r[1] - st0 >= k * 10.5 && r[1] - st0 < (k + 1) * 10.5); const env = []; for (let s = 0; s < 10; s++) { const ww = w.filter((r) => r[1] - st0 - k * 10.5 >= s && r[1] - st0 - k * 10.5 < s + 1); env.push(rel(mean(ww.map((r) => r[4])))); } return { loudest250ms: rel(movmax(w, 4, 12)), loudest250msK: rel(movmax(w, 5, 12), 1), perSecond: env }; }); const tr = ph('train', 2); out.train = { loudest250ms: rel(movmax(tr, 4, 12)), loudest250msK: rel(movmax(tr, 5, 12), 1), byDistance: {} }; const bins = {}; for (const r of tr) { const b = r[8] < 0 ? 'none' : r[8] < 20 ? 'a <20m' : r[8] < 40 ? 'b 20-40m' : r[8] < 80 ? 'c 40-80m' : r[8] < 150 ? 'd 80-150m' : 'e >150m'; (bins[b] = bins[b] || []).push(r); } for (const [b, rows] of Object.entries(bins)) out.train.byDistance[b] = { n: rows.length, mean: rel(mean(rows.map((r) => r[4]))), meanK: rel(mean(rows.map((r) => r[5])), 1) }; for (const P of ['chirp', 'chirpK']) { const ch = ph(P, 1); const cb = {}; for (const r of ch) { const b = r[9] < 0 ? 'none' : r[9] < 12 ? 'a <12m' : r[9] < 24 ? 'b 12-24m' : r[9] < 36 ? 'c 24-36m' : r[9] < 46 ? 'd 36-46m' : 'e >46m'; (cb[b] = cb[b] || []).push(r); } out[P] = {}; for (const [b, rows] of Object.entries(cb)) out[P][b] = { n: rows.length, mean: rel(mean(rows.map((r) => r[4]))), loudest100ms: rel(movmax(rows, 4, 5)), meanK: rel(mean(rows.map((r) => r[5])), 1) }; } out.signal = window.__SIG__; out.rows = R.length; out.citySoundWarned = !!window.__DEBUG__.audio._cityWarned; return out; })()"}
]
