[{"click":".map[data-m=city]"},{"wait":6000},{"click":".diff[data-d=hard]"},{"wait":3000},{"click":"#startb"},{"wait":1200},{"js":"(async () => { const D = window.__DEBUG__, G = D.G, t = D.track, car = D.car; const sleep = (ms) => new Promise((r) => setTimeout(r, ms)); let hits = 0; const hw = car.hitWall.bind(car); car.hitWall = (...a) => { const v = hw(...a); if (v > 1.5) hits++; return v; }; const pins = t.features.filter((f) => (f.type === 'hairpin' || f.type === 'ess') && t.pts[f.i0].s > 250).slice(0, 10); const rows = []; for (const f of pins) { let kmax = 0; for (let i = f.i0; i <= f.i1; i++) kmax = Math.max(kmax, Math.abs(t.pts[i].k)); let ie = f.i0; while (ie < f.i1 && Math.abs(t.pts[ie].k) < 0.6 * kmax) ie++; let ix = ie; while (ix < f.i1 && Math.abs(t.pts[ix].k) > 0.3 * kmax) ix++; const sE = t.pts[ie].s, sX = t.pts[ix].s; D.teleport(sE - 45, 55); hits = 0; let wide = -9, T = 0, flick = -1, spin = 0, tExit = -1, maxB = 0; window.__AUTOPILOT__ = (dt) => { T += dt; const c = car, n = t.nearest(c.x, c.z, G.idx); const ahead = t.sample(G.s + 7 + c.speed * 0.45); let e = ahead.h - c.yaw; e = Math.atan2(Math.sin(e), Math.cos(e)); const kd = Math.sign(t.sample(sE + 5).k); const b = Math.abs(n.u) - (n.u >= 0 ? n.wl : n.wr); if (G.s > sE - 3 && b > wide) wide = b; maxB = Math.max(maxB, Math.abs(c.beta)); if (Math.abs(c.beta) > 1.45) spin = 1; if (tExit < 0 && G.s > sX) tExit = T; if (flick < 0 && G.s > sE - 7) flick = T; const inFlick = flick >= 0 && T - flick < 0.35; let steer = inFlick ? kd : Math.max(-1, Math.min(1, e * 2.2 - n.u * 0.08)); return { throttle: inFlick ? 0.3 : (G.s < sE - 7 ? (c.speed < 15.3 ? 1 : 0) : 0.75), brake: 0, steer, hand: inFlick ? 1 : 0, reverse: false, touch: false }; }; await sleep(6000); rows.push([Math.round(sE), Math.round(1 / kmax), hits, +wide.toFixed(1), tExit < 0 ? 'no exit' : +(tExit - flick).toFixed(2), spin, +maxB.toFixed(2)]); } window.__AUTOPILOT__ = null; const ok = rows.filter((r) => typeof r[4] === 'number'); const sum = { corners: rows.length, exited: ok.length, hits: rows.reduce((a, r) => a + r[2], 0), wideAvg: +(rows.reduce((a, r) => a + r[3], 0) / rows.length).toFixed(2), exitAvg: ok.length ? +(ok.reduce((a, r) => a + r[4], 0) / ok.length).toFixed(2) : null, spins: rows.reduce((a, r) => a + r[5], 0) }; return { sum, rows }; })()"}]