[
  { "click": ".map[data-m=city]" },
  { "wait": 4000 },
  { "click": "#startb" },
  { "wait": 1500 },
  { "js": "(() => { const D = window.__DEBUG__; D.G.hour = 13; D.G.hourShown = 12.9; D.rainNow(0); D.teleport(420, 0); })()" },
  { "wait": 3500 },
  { "js": "(() => { const D = window.__DEBUG__; window.__EMIT__ = (kind, k) => { const out = []; D.world.root.traverse((o) => { if (o.name === 'steam' && o.geometry) { const P = o.geometry.attributes.position, A = o.geometry.attributes.aP; for (let i = 0; i < P.count; i += 4) if (Math.round(A.getZ(i)) === kind) out.push([P.getX(i), P.getY(i), P.getZ(i)]); } }); const c = D.car; out.sort((a, b) => Math.hypot(a[0] - c.x, a[2] - c.z) - Math.hypot(b[0] - c.x, b[2] - c.z)); return out[k] || null; }; const e = window.__EMIT__(0, 0); const p = D.track.sample(D.track.nearest(e[0], e[2], D.G.idx).s); const fx = Math.sin(p.h), fz = Math.cos(p.h); window.__CAM__ = { pos: [e[0] - fx * 9, e[1] + 1.8, e[2] - fz * 9], look: [e[0], e[1] + 1.4, e[2]], fov: 60 }; return D.G.hourShown; })()" },
  { "wait": 1200 },
  { "shot": "D1_day_steam" },
  { "js": "(() => { const D = window.__DEBUG__; const out = []; D.world.root.traverse((o) => { if (o.name === 'people' && o.geometry) { const P = o.geometry.attributes.position; for (let i = 0; i < P.count; i += 4) out.push([P.getX(i), P.getY(i), P.getZ(i)]); } }); const c = D.car; out.sort((a, b) => Math.hypot(a[0] - c.x, a[2] - c.z) - Math.hypot(b[0] - c.x, b[2] - c.z)); const e = out[0]; const n = D.track.nearest(e[0], e[2], D.G.idx); const p = D.track.sample(n.s); window.__CAM__ = { pos: [p.x, p.y + 1.6, p.z], look: [e[0], e[1] + 1.1, e[2]], fov: 40 }; })()" },
  { "wait": 900 },
  { "shot": "D2_day_people" },
  { "js": "(() => { window.__CAM__ = null; window.__DEBUG__.teleport(700, 40); })()" },
  { "keys": ["KeyW"], "ms": 2000 },
  { "shot": "D3_day_drive" }
]
